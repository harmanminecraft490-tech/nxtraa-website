# TODO (BlackboxAI session)

## Admin Panel (High priority)
- [ ] Fix Admin UI to support: create/add product, delete product, and proper edit/update payloads.
- [ ] Fix API mismatch: ensure admin-client uses the correct endpoints and payload fields.
- [ ] Add missing API routes for admin: DELETE product, POST add product.
- [ ] Make image delete/remove persist to DB (not just client-side).
- [ ] Fix upload UI and admin product schema mapping (rating/oldPrice types, etc.).
- [ ] Add dashboard stats endpoint using real DB values.

## Mobile UI cleanup
- [ ] Reduce navbar height and spacing on small screens.
- [ ] Ensure all admin/product lists avoid horizontal scroll; adjust card sizes for mobile.

## Project cleanup
- [ ] Remove debug logs and unused imports where found.

